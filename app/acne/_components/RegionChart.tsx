'use client';

// TODO (Bloco 7): implementar RegionChart completo com Recharts

import type { RegionCount } from '../_lib/countByRegion';

export interface RegionChartProps {
  regionCounts: RegionCount[];
}

export default function RegionChart(_props: RegionChartProps) {
  // TODO (Bloco 7): Recharts BarChart horizontal, REGION_LABELS_PT, cores --mod-acne, responsivo
  return <div>Gráfico por região (em construção)</div>;
}
